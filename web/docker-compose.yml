version: '3.8'

services:
  db:
    # ... (db service, sem alterações)
    
  api:
    # ... (api service, sem alterações)

  frontend:
    build:
      context: ./frontend 
      dockerfile: Dockerfile
    container_name: ftr-pos-frontend
    restart: always
    depends_on:
      - api
    ports:
      - "5174:5174" 
    # 🎯 ADICIONE ESTE BLOCO PARA LER O .ENV DA RAIZ
    env_file:
      - ./.env 
    environment:
      # ⚠️ Esta variável é lida no build para configurar o código do frontend
      - VITE_APP_API_URL=http://api:${PORT} 