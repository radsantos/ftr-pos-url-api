services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    container_name: ftr-pos-web
    ports:
      - "5173:5173"
    environment:
      - VITE_BACKEND_URL=http://api:3000
    depends_on:
      - api
    restart: always
    